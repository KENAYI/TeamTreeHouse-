# Search through array

new_list = [1,2,3]
result = new_list[0]

# Check that first index in array contains "1"
for n in new_list:
    if n == 1:
        print (True)
        break
    

numbers = []
numbers.extend([4,5,6])
print(numbers)